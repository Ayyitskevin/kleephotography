# Host hardening

Host-level hardening for the production box (firewall, sshd, fail2ban, service and
data-directory permissions) is machine-specific infrastructure, not application code —
it lives in the operator’s private ops notes, outside this repo.
